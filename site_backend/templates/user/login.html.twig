<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>tienda |vc-entrega.com</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="{{ asset('plugins/fontawesome-free/css/all.min.css') }}">
    <!-- Theme style -->
    <link rel="stylesheet" href="{{ asset('css/adminlte.min.css') }}">

</head>
<body class="hold-transition login-page" style="background-image: url({{ asset('img/fondo.svg') }});
        background-repeat: repeat;
        background-size: 500px 500px;">
<div class="login-box">
    <div class="login-logo">
        <a href="{{ path('index') }}"><img style="max-height: 300px" src="{{ asset('img/logo.png') }}"
                                           class="brand-image img-circle">
        </a>
    </div>
    <!-- /.login-logo -->
    <div class="card">
        <div class="card-body login-card-body">
            {% block formulario %}
            <h5 class="mb-3 font-weight-normal">Regístrse para iniciar su sesión.</h5>
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.session.get('verificaemail') %}
                    <div class="alert alert-info">Verifica el enlace que hemos enviado a tu
                        correo: {{ app.session.get('verificaemail') }} para activar tu
                        cuenta.
                    </div>
                {% endif %}

                {% if app.session.get('verificado') %}
                    <div class="alert alert-success">Cuenta verificada correctamente.
                    </div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        Ya iniciaste sesión como {{ app.user.username }}, <a href="{{ path('app_logout') }}"> Salir </a>
                    </div>
                {% endif %}

                <label for="inputUsername">usuario</label>
                <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control"
                       required autofocus>
                <label for="inputPassword">contraseña</label>
                <input type="password" name="password" id="inputPassword" class="form-control" required>

                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}">
                <br>

                <button class="btn btn-lg btn-primary" STYLE="float: right" type="submit">
                    Iniciar
                </button>
            </form>

            <p class="mb-0">
                <a href="{{ path('app_register') }}" class="text-center">Crear cuenta nueva.</a>
            </p>
        </div>
        <!-- /.login-card-body -->
        {% endblock %}

    </div>
</div>

<h5 class="text-center p-4">Copyright &copy; 2021 <a href="{{ path('index') }}">tienda.vc-entrega.com</a>.</h5>

<!-- /.login-box -->

<!-- REQUIRED SCRIPTS -->

<!-- jQuery -->
<script src="{{ asset('plugins/jquery/jquery.js') }}"></script>
<!-- Bootstrap 4 -->
<script src="{{ asset('plugins/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
<!-- AdminLTE App -->
<script src="{{ asset('js/adminlte.js') }}"></script>

</body>
</html>